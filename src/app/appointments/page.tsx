import AppointmentsList from "../../components/appointments/AppointmentsList";

const AppointmentsPage = () => {
  return (
    <div className="min-h-screen bg-slate-300 pt-6 pb-12">
      <AppointmentsList />
    </div>
  );
};

export default AppointmentsPage;
